# 从零发布这个网站

写给完全没用过 GitHub 的人。全程在浏览器里点，不需要装任何软件，不需要写代码。
大约 20 分钟。**每一步都有「你应该看到什么」，对不上就说明这一步没成，别往下走。**

准备一个 Anthropic 账号（下面第 4 步会用），其余什么都不用准备。

---

## 第 1 步 · 把代码合并到主分支

现在代码在一个叫 `claude/lucas-daily-news-filter-06k81t` 的分支上。
GitHub 的定时任务**只认主分支（main）**，所以必须先合并过去。

1. 打开 https://github.com/JinYEAH123/LucasDailyNews
2. 页面上方有一条黄色提示条，写着分支名和一个绿色按钮 **Compare & pull request**，点它。
   - 没看到黄条？点顶部的 **Pull requests** → **New pull request**，
     左边选 `main`，右边选 `claude/lucas-daily-news-filter-06k81t`。
3. 进入的页面上点绿色的 **Create pull request**。
4. 页面刷新后，点绿色的 **Merge pull request**，再点 **Confirm merge**。

> **你应该看到什么**：紫色的 “Merged” 字样。
> 回到仓库首页，左上角分支选择器显示 `main`，文件列表里能看到
> `config.toml`、`scripts`、`docs` 这些。

---

## 第 2 步 · 允许机器人写回仓库

每天生成完新闻，任务需要把结果存回仓库。默认权限可能不够，会报错。

1. 仓库页面右上 **Settings**（齿轮图标那一栏，不是你头像下的 Settings）
2. 左侧菜单往下找 **Actions** → 点它下面的 **General**
3. 滚到页面最下面 **Workflow permissions**
4. 选 **Read and write permissions**
5. 点 **Save**

> **你应该看到什么**：“Read and write permissions” 前面的圆点是实心的。

---

## 第 3 步 · 打开网页托管

1. 还在 **Settings** 里，左侧菜单点 **Pages**
2. **Source** 选 **Deploy from a branch**
3. 下面出现两个下拉框：左边选 **main**，右边选 **/docs**
4. 点 **Save**

等一两分钟，刷新这个页面，顶部会出现一句
“Your site is live at https://jinyeah123.github.io/LucasDailyNews/”。

> **你应该看到什么**：点开那个网址，能看到「Daily News for Kids」的页面。
> 如果显示 404，多等两分钟再刷新；GitHub 第一次部署有时要三五分钟。

**把这个网址复制下来，下一步要用。**

---

## 第 4 步 · 把网址填进配置

长图上的二维码指向这个网址，不填的话扫出来是空的。

1. 回到仓库首页，点文件列表里的 **config.toml**
2. 点右上角的铅笔图标 ✏️（Edit this file）
3. 找到最下面这两行：
   ```
   [site]
   url = "https://jinyeah123.github.io/LucasDailyNews/"
   ```
   确认引号里就是第 3 步拿到的网址。顺便可以改上面的
   `name`（孩子名字）、`age`（年龄，5-18 岁）等等。
4. 点右上角绿色的 **Commit changes...**，再点 **Commit changes**

> **你应该看到什么**：回到文件页面，内容已经是你改过的样子。

---

## 第 5 步 · 填入 API 密钥

这是唯一要花钱的地方，每天大约一美元上下。

**先去拿密钥：**

1. 打开 https://console.anthropic.com 注册或登录
2. 左侧 **API keys** → **Create Key**，起个名字，点创建
3. **立刻复制**弹出来的那串字符（以 `sk-ant-` 开头）。
   关掉窗口就再也看不到了，只能重新创建。
4. 同一个网站左侧 **Billing**，充值一点余额（没有余额的话调用会失败）

**再填进仓库：**

1. 回到仓库 **Settings** → 左侧 **Secrets and variables** → **Actions**
2. 点绿色的 **New repository secret**
3. **Name** 填：`ANTHROPIC_API_KEY`（一个字都不能错，全大写，下划线）
4. **Secret** 粘贴刚才复制的密钥
5. 点 **Add secret**

> **你应该看到什么**：列表里出现一行 `ANTHROPIC_API_KEY`。
> 看不到值是正常的，GitHub 存进去之后就不再显示了。

---

## 第 6 步 · 先跑一次试试

不用等到设定的时刻，可以手动触发。

1. 仓库顶部点 **Actions**
2. 第一次进来可能有一句 “Workflows aren't being run on this forked repository”
   或者一个绿色按钮 **I understand my workflows, go ahead and enable them** —— 点它
3. 左侧点 **Daily edition**
4. 右边点 **Run workflow** → 再点弹出来的绿色 **Run workflow**
5. 等几秒刷新页面，会出现一条黄色的运行记录，点进去可以看进度

整个过程大约 5–10 分钟（要联网检索、写稿、生成长图）。

> **你应该看到什么**：所有步骤前面变成绿色的勾。
> 然后打开你的网址，看到今天的新闻。

**如果变成红色的叉**，点开那个红色的步骤看报错，常见原因：
- `ANTHROPIC_API_KEY` 名字拼错，或者账号没余额
- 第 2 步的写入权限没设 → 报错里会有 `permission denied` 或 `403`

---

## 第 7 步 · 邮件推送（可选，可以以后再弄）

**先拿一个「应用专用密码」——不要用你邮箱的登录密码。**

Gmail：
1. 打开 https://myaccount.google.com/security ，确认 **两步验证** 已开启（没开就先开）
2. 打开 https://myaccount.google.com/apppasswords
3. 起个名字比如 `news`，点创建，复制那串 16 位字母

其他邮箱：Outlook 用 `smtp-mail.outlook.com`，iCloud 用 `smtp.mail.me.com`，
同样都需要各自的「应用专用密码」。

**再回到仓库 Settings → Secrets and variables → Actions，逐个添加：**

| Name | Secret |
| --- | --- |
| `SMTP_HOST` | `smtp.gmail.com` |
| `SMTP_PORT` | `587` |
| `SMTP_USER` | 你的完整邮箱地址 |
| `SMTP_PASSWORD` | 刚才那 16 位应用专用密码 |
| `NEWSLETTER_RECIPIENTS` | 见下面 |

`NEWSLETTER_RECIPIENTS` 的写法，逗号分隔，冒号后面是给这个人发哪种语言：

```
lucas@example.com:en, mum@example.com:zh, grandpa@example.com:zh
```

以后要加人或减人，只改这一个 secret 就行。

> **你应该看到什么**：再跑一次第 6 步，几分钟后邮箱里收到当天那一期。

---

## 完成之后

- **网址**：`https://jinyeah123.github.io/LucasDailyNews/`
- **自动更新**：每天你设定的那个时刻，不用管
- **改设置**：网页最下面有个「做一份自己的」链接，点开填表单，
  照着填完把内容复制，粘回仓库的 `config.toml`（重复第 4 步的操作）
- **换阅读难度**：网页上方有个切换条（6-11 岁 / 12-15 岁 / 16+ 岁），
  谁都能点，点了立刻切换，不用改设置
- **转发长图**：网页顶部「转发长图」按钮，长按保存，发朋友圈

---

## 常见问题

**网址打开是 404**
Pages 第一次部署要几分钟。超过十分钟还不行，回第 3 步确认分支选的是
`main`、目录选的是 `/docs`。

**任务没有按时跑**
GitHub 的定时任务经常延迟几分钟到半小时，这是正常的。这个任务每小时检查一次，
只要当天还没生成就会补上，所以晚一点会自己补，不会丢。

**红色的叉，报错里有 403 或 permission**
第 2 步的写入权限没设好。

**红色的叉，报错里有 credit 或 balance**
Anthropic 账号没余额了，去 Billing 充值。

**仓库很久没动，定时任务停了**
GitHub 对超过 60 天没有任何提交的仓库会暂停定时任务。这个工具每天都会提交，
所以正常用不会触发。真被停了，进 Actions 手动跑一次就会恢复。

**想让别人也用**
让他们在你的仓库页面点右上角 **Fork**，然后从第 2 步开始做一遍，
用他们自己的 API 密钥和自己的 `config.toml`。
